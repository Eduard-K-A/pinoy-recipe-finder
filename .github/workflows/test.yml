name: React + Vite CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4  # Updated to v4 for better performance

      - name: Use Node.js 18
        uses: actions/setup-node@v4  # Updated to v4
        with:
          node-version: 18
          cache: 'npm'  # Built-in npm caching

      - name: Cache npm dependencies
        uses: actions/cache@v4  # Updated to v4
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4  # Fixed: Updated to v4 to resolve deprecation
        with:
          name: build
          path: dist/
